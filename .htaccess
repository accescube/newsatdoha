# Apache configuration for NEWS @ DOHA
AddDefaultCharset UTF-8

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /NEWSATDOHA/
    
    # Prevent directory listing
    Options -Indexes
</IfModule>
